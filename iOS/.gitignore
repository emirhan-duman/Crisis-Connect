.DS_Store

# Local build and tool artifacts
.codex-derived*/
.spm-fresh-cache/
.tmp_crashlogs/
.tmp_devicelogs/
DerivedData/
.build/

# Xcode user-specific state
xcuserdata/
*.xcuserstate
*.hmap

# Xcode build/packaging
*.ipa
*.dSYM.zip
*.dSYM

# Playgrounds
timeline.xctimeline
playground.xcworkspace

# Carthage
Carthage/Build/

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Local profiling and debug captures
*.trace
*-os-log.xml
*-process-info.xml

# Rust MLS core (OrangeMlsWorker.xcframework) is now tracked via git-lfs — see .gitattributes
# and docs/SFU_GROUP_CALLS.md. Rebuild with crisis-connect-web/rust-mls-ios/build-xcframework.sh.

# Local build output (xcodebuild -derivedDataPath, exported .app bundles)
build-artifacts/
